This repository does not store raw Olist dataset files.

Raw batch files should be staged in GCS under:
gs://<bucket>/olist/raw/dt=YYYY-MM-DD/<table>.csv

Use the ingestion scripts to upload or generate batch files locally.